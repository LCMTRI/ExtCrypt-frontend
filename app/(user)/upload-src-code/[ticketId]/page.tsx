import UploadSrcCodePageComponent from "@/page-components/upload-src-code";
import { useParams } from "next/navigation";

const page = () => {
  return <UploadSrcCodePageComponent />;
};

export default page;
